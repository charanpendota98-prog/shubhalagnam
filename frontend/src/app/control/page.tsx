"use client";

/**
 * 🎛️ CONTROL PORTAL HOME — advanced operations dashboard.
 * Owner + worker (RBAC), real backend analytics + actions via session cookie.
 */
import Dashboard from "@/components/control/Dashboard";

export default function ControlHome() {
  return <Dashboard />;
}
