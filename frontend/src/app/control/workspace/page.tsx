"use client";

/**
 * 👷 WORKER WORKSPACE — same advanced dashboard, RBAC hides owner-only tabs
 * (revenue + audit). Workers see overview, profile queue and moderation reports.
 */
import Dashboard from "@/components/control/Dashboard";

export default function Workspace() {
  return <Dashboard />;
}
